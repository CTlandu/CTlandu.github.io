# 快速开始指南

## ✅ 已完成的重构工作

我已经将你的 Jekyll 项目重构为 React + TailwindCSS 项目，包括：

1. ✅ **项目基础结构**
   - React + Vite 配置
   - TailwindCSS 配置
   - 路由系统（React Router）

2. ✅ **页面迁移**
   - 首页（About）内容已迁移
   - 摄影作品页面框架已创建
   - 出版物页面已迁移

3. ✅ **功能实现**
   - 响应式导航栏
   - 深色模式支持
   - 移动端适配

4. ✅ **部署配置**
   - GitHub Actions 自动部署工作流
   - GitHub Pages 配置说明

## 🚀 如何运行

### 1. 安装依赖

```bash
npm install
```

### 2. 启动开发服务器

```bash
npm run dev
```

访问 `http://localhost:5173`

### 3. 构建生产版本

```bash
npm run build
```

## 📦 部署到 GitHub Pages

### 方法一：自动部署（推荐）

1. 在 GitHub 仓库中：
   - 进入 **Settings** → **Pages**
   - **Source** 选择 **"GitHub Actions"**

2. 推送代码到 `main` 分支：
   ```bash
   git add .
   git commit -m "Migrate to React + TailwindCSS"
   git push origin main
   ```

3. GitHub Actions 会自动构建和部署

### 方法二：手动部署

```bash
npm run deploy
```

## 📝 待完成的工作

虽然基础结构已经完成，但还需要：

1. **迁移项目详情**
   - 从 `_projects/*.md` 文件中提取内容
   - 创建项目详情页面组件
   - 实现图片轮播功能

2. **迁移静态资源**
   - 确保所有图片在 `public/assets/img/` 目录
   - 检查 PDF 文件路径

3. **完善功能**
   - 添加图片懒加载
   - 优化移动端体验
   - 添加加载动画

## 🔧 项目结构

```
├── src/
│   ├── components/      # React 组件
│   │   └── Layout.jsx   # 布局组件
│   ├── pages/          # 页面组件
│   │   ├── Home.jsx
│   │   ├── Photography.jsx
│   │   └── Publications.jsx
│   ├── data/           # 数据文件
│   │   └── projects.json
│   ├── App.jsx         # 主应用
│   └── main.jsx        # 入口文件
├── public/             # 静态资源
│   └── assets/        # 图片、PDF 等
├── package.json
├── vite.config.js
└── tailwind.config.js
```

## ⚠️ 重要提示

1. **图片路径**：所有图片应该放在 `public/assets/img/` 目录下，使用绝对路径引用（如 `/assets/img/prof_pic.png`）

2. **路由**：当前使用 `HashRouter`，URL 会包含 `#`（如 `/#/photography`）。如果想使用干净 URL，需要配置 `BrowserRouter` 和 `404.html`

3. **Jekyll 文件**：原有的 Jekyll 文件可以保留作为备份，不会影响 React 项目运行

## 🎨 自定义样式

TailwindCSS 配置在 `tailwind.config.js` 中，可以：
- 修改主题颜色
- 添加自定义工具类
- 配置响应式断点

## 📚 更多信息

- 详细迁移指南：查看 `MIGRATION-GUIDE.md`
- React 项目说明：查看 `README-REACT.md`

